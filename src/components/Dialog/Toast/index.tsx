import React from "react";

type ToastProps = {

}

const Toast: React.FC<ToastProps> = (props: ToastProps) => {

    return (
        <div>
            Toast
        </div>
    )
}

export default Toast;